# Project Template

## Building

For the debug build, run

```shell
cmake -B cmake-build-debug -DCMAKE_BUILD_TYPE=Debug
cmake --build cmake-build-debug
```

while for the release build, run

```shell
cmake -B cmake-build-debug -DCMAKE_BUILD_TYPE=Release
cmake --build cmake-build-debug
```